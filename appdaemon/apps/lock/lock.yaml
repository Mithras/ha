Lock:
  module: lock
  class: Lock
  global_dependencies:
    - globals
  dependencies:
    - Common
  config:
    name: Front Door
    state: front_door
    code_slot: 1
    lock: lock.schlage_connect_smart_deadbolt_locked
    access_control_sensor: sensor.schlage_connect_smart_deadbolt_access_control
    user_code_sensor: sensor.schlage_connect_smart_deadbolt_user_code
